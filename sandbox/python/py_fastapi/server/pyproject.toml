[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.124.4",
    "uvicorn>=0.38.0",
]

[tool.uv.workspace]
members = ["bla"]
