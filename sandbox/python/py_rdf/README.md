# Triples

- `subject` -> the resource being described (e.g., "Bob")
- `predicate` -> the property or relationship (e.g., "knows")
- `object` -> the value or another resource (e.g., "Alice" or a literal like a number or string)
