[project]
name = "playwright-multi-window-poc"
version = "0.1.0"
description = "A POC for cross-window drag-and-drop using Playwright"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.uv]
package = false
