version: '3'

tasks:
  run:
    desc: Run the application
    cmds:
      - zig build run

  test:
    desc: Run tests
    cmds:
      - zig build test

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf zig-out
      - rm -rf .zig-cache
