version: '3'

tasks:
  default:
    cmds:
      - task --list

  run:
    aliases: [r]
    desc: "Run main application"
    cmds:
      - cargo run
      
  build:
    aliases: [b]
    desc: "Build main application"
    cmds:
      - cargo build