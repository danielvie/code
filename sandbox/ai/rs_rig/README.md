# Rig example

## install crate

install rig
```sh
cargo add rig
# or: cargo add rig-core
```

install tokio and enable tokio's `macros` and `rt-multi-thread`
```sh
cargo add tokio --features macros,rt-multi-thread
```

install anyhow
```sh
cargo add anyhow
```