
`Package`: A Cargo project containing one or more build targets (`Cargo.toml`)
`Crate`  : One compilation unit, producing a library or executable (`src/lib.rs`, `src/main.rs`)
`Module` : A namespace used to organize code inside a crate (`mod`, `pub mod`)